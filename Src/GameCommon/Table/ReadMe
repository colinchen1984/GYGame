Table File采用如下设计规则：
	1. 字符串采用utf8编码
	2. 只兼容unix和window两种格式的换行符
	3. 该库不是完全线程安全, 可以多线程访问同一张表，但是从库的初始化以及从文件中读取数据的操作请不要使用线程
	4. table file应该采用utf-8编码方式存储
	5. 支持行注释(在行首(并且只在行首有效)使用字符#即可注释该行)
	6. 如果需要检查表格内数据类型的定义时候和代码内的数据定义吻合，请打开CHECK_DATA_TYPE宏
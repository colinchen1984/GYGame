网络协议采用如下设计规则：
	1. 字符串采用utf8编码
	2. 所有的数据均不使用一字节对齐，在包内手流化
	3. 所有协议均使用ProtocolTool自动生成，没有特殊需求不要手工输入代码
	
网络包工具注意事项：
	1. 生成的代码的文件编码格式有template文件的编码格式决定
	2. 代码中的注释的编码格式有11文件编码格式决定
	3. 如果代码中不能正确显示中文注释，请查template和11文件的编码格式是否一致